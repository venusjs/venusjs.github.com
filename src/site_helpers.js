// var helperUtils = require('punch').Utils.Helper;

// module.exports = {
  // get: function (basepath, content_type, request_options, callback) {
    // var error, blockHelpers, tagHelpers;

    // error = null;

    // blockHelpers = {
      // 'codejs': function () {
        // return helperUtils.checkArgs(arguments, function (text) {
          // return text + 'satoeu saotehu asonteuh asnotehu ';
        // });
      // }
    // };

    // tagHelpers = {};

    // return callback(null, {"tag": tagHelpers, "block": blockHelpers});
  // }
// };
